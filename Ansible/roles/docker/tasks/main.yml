# docker configuration

# - name: Update the apt package
#   apt: update_cache=yes

# - name: Install packages to allow apt to use a repository over HTTPS
#   apt: 
#     name:
#       - apt-transport-https
#       - ca-certificates
#       - curl
#       - gnupg-agent
#       - software-properties-common

# - name: Add Docker’s official GPG key
#   shell: curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
#     | sudo apt-key add -

# - name: Set up the stable repository
#   apt_repository:
#     repo: deb [arch=amd64] https://download.docker.com/linux/ubuntu \
#       xenial \
#       stable
    
# - name: Update the apt package
#   apt: update_cache=yes


- name: Install the latest version of Docker CE 
  apt:  
    name:
      - docker-ce
    state: present

# - name: Install docker-compose = 2.0.*
#   apt:  
#     name: docker-compose = 2.0.*
#     state: present
#     update_cache: yes
#   tags: 
#     - docker

# - name: Install docker via pip
#   pip:
#     name: 
#       - docker
#       - requests>=2.20.1 







